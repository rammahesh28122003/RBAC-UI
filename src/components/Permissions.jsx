import React from 'react';
import './Permissions.css';

const Permissions = () => {
  return (
    <div className="permissions">
      <center><h2>Permissions Management</h2></center>
      <p>Here you can dynamically assign or modify permissions for roles.</p>
      <p>(Functionality for assigning permissions can be extended.)</p>
    </div>
  );
};

export default Permissions;
